{
  "name": "projeto-reforma",
  "version": "0.1.0",
  "private": true,
  "description": "Orquestração do monorepo: sobe Postgres, backend e frontend com um comando só. Não contém código de aplicação.",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm --prefix backend run prisma:generate",
    "db:up": "docker compose --env-file backend/.env -f backend/docker-compose.yml up -d --wait",
    "db:down": "docker compose --env-file backend/.env -f backend/docker-compose.yml down",
    "dev": "npm run db:up && concurrently -k -n backend,frontend -c cyan,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend start\"",
    "stop": "node scripts/stop.mjs",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
